pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "GemTest"
include ':app'
include ':gemstone-android'
project(':gemstone-android').projectDir = new File(settingsDir, '../../../android/gemstone')
