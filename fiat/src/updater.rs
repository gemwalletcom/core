//use storage::{database::{DatabaseClient, self}};

pub struct Updater {
    //database: DatabaseClient,
}

impl Updater {
    pub async fn new(//database_url: &str,
    ) -> Self {
        //let database = DatabaseClient::new(database_url);
        Self {
            //database,
        }
    }
}
